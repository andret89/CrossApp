<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:userControls="clr-namespace:CrossApp.ViewModels"
             x:Class="CrossApp.Views.ListPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Logout" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
   
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <ListView  x:Name="lvInterventi" ItemsSource="Interventi" HasUnevenRows="True" ItemTapped="OnTapIntervento" 
                       ItemTemplate="{StaticResource ListInterventiItemTemplate}" 
                       IsPullToRefreshEnabled="True" RefreshCommand="{Binding RefreshCommand}" 
                       IsRefreshing="{Binding IsRefreshing}" SeparatorVisibility="None"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>